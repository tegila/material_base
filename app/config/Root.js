import React from 'react';
import { HashRouter } from 'react-router-dom';
import App from '../components/App';

const Root = () => {
  return (
    <HashRouter>
      <App />
    </HashRouter>
  );
};

export default Root;

